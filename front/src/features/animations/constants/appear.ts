// Délai (en millisecondes) entre chaque élément lors de l'apparition en séquence (stagger animation)
export const APPEAR_STAGGER_MS = 400;

// Classe Tailwind pour définir la durée de transition/animation de l'apparition
export const APPEAR_DURATION_CLASS = "duration-[1000ms]";

// Ratio de visibilité de l'élément dans le viewport avant de déclencher l'animation (0 = pas du tout visible, 1 = totalement visible)
export const APPEAR_THRESHOLD = 0.2;

// Délai entre chaque carte projet (page /projects) pour l’apparition en cascade
export const PROJECT_CARD_STAGGER_MS = 100;
